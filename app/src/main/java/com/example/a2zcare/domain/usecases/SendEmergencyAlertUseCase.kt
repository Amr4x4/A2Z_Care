package com.example.a2zcare.domain.usecases
/*
import com.example.a2zcare.domain.entities.LocationData
import com.example.a2zcare.domain.model.Result
import com.example.a2zcare.domain.repository.LocationSharingRepository
import javax.inject.Inject

class SendEmergencyAlertUseCase @Inject constructor(
    private val repository: LocationSharingRepository
) {
    suspend operator fun invoke(
        userId: String,
        location: LocationData,
        message: String = "I need help! This is my current location."
    ): Result<Unit> {
        return repository.sendEmergencyAlert(userId, location, message)
    }
}


 */